# Root .env — LLM keys shared by sim_chat CLI and backend (via load_dotenv chain).
# Copy to .env and fill in your keys.

OPENAI_API_KEY=your_openai_api_key_here

# Optional: switch back to Gemini
# LLM_PROVIDER=gemini
# GOOGLE_API_KEY=your_google_api_key_here

# =============================================================================
# Database: configure in backend/.env (not here).
# Local:  postgresql+psycopg://fss:fss%40123@localhost:5432/debating
# Cloud:  Supabase URL — see backend/.env.example
# =============================================================================
