# Optional: call Railway directly instead of same-origin /api (local dev or Vercel proxy).
# Leave unset for local dev (Vite proxies /api) and Vercel prod (vercel.json rewrites /api).
# Host only — no trailing slash, no /api suffix.
# VITE_API_BASE_URL=https://debating-production.up.railway.app
